# 安装过程提示（每行一条，安装时轮换显示；以 # 开头的行不显示）

安装期间请勿关闭本窗口。
离线安装不访问网络，全部资源已内置。
记忆文件为纯文本，位于同步库（vault）的 HerMemory\memory\ 下。
修改记忆文件后，开启新对话生效。
修改 API 配置后，需重启 Gateway 生效。
常见问题见 docs\GUIDE.md。
安装完成后可在系统托盘启动或停止 Gateway。
卸载不删除同步库，文档与记忆默认保留。
