# 安装过程提示（每行一条，安装时轮换显示；以 # 开头的行不显示）

首次安装约 5-10 分钟，主要时间用于下载内核与依赖。
安装完成后，AI 将接入微信，对话即可使用全部功能。
记忆文件为纯文本，位于同步库（~/vault）的 HerMemory\memory\ 下。
修改记忆文件后，开启新对话即可生效。
修改配置（API 地址、Key）后，需要重启网关。
遇到问题可查看 docs\GUIDE.md 的常见问题。
安装完成后可随时在系统托盘启动或停止。
卸载不会删除同步库，你的文档与记忆默认保留。
